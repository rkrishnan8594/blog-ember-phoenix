<ul>
  {{#each model as |post|}}
    <li>
      <h3>{{post.title}}</h3>
      <p>by {{post.author}}</p>
      <p>{{post.summary}}</p>
      <p>{{post.comments.length}} Comments</p>
    </li>
  {{/each}}
</ul>

{{outlet}}
